services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5174:5174"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=5174
    restart: unless-stopped
